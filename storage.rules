// Reglas de seguridad de Firebase Storage
// Modo de prueba - Válido hasta el 31 de enero de 2026
// 
// IMPORTANTE: Estas reglas permiten acceso de lectura/escritura a todos los usuarios
// durante el período de desarrollo. Debes actualizar estas reglas antes de producción.
//
// Para configurar estas reglas en Firebase Console:
// 1. Ve a Firebase Console > Storage
// 2. Selecciona la pestaña "Reglas"
// 3. Copia y pega el siguiente código
// 4. Haz clic en "Publicar"

rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if
          request.time < timestamp.date(2026, 1, 31);
    }
  }
}

// ============================================================
// REGLAS DE PRODUCCIÓN (Usar después del desarrollo)
// ============================================================
// Descomentar y usar estas reglas cuando la app esté lista para producción:

/*
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    
    // Reglas para fotos de perfil
    match /usuarios/{userId}/perfil/{fileName} {
      // Permitir lectura a todos los usuarios autenticados
      allow read: if request.auth != null;
      
      // Solo el usuario puede subir/actualizar su foto de perfil
      allow write: if request.auth != null 
        && request.auth.uid == userId
        && request.resource.size < 5 * 1024 * 1024  // Máximo 5MB
        && request.resource.contentType.matches('image/.*');  // Solo imágenes
    }
    
    // Reglas para imágenes de recetas
    match /recetas/{recetaId}/{fileName} {
      // Permitir lectura a todos los usuarios autenticados
      allow read: if request.auth != null;
      
      // Solo usuarios autenticados pueden subir imágenes de recetas
      allow create: if request.auth != null
        && request.resource.size < 10 * 1024 * 1024  // Máximo 10MB
        && request.resource.contentType.matches('image/.*');
      
      // Solo el creador puede actualizar/eliminar
      allow update, delete: if request.auth != null;
    }
    
    // Reglas para archivos temporales
    match /temp/{userId}/{fileName} {
      // Solo el usuario puede acceder a sus archivos temporales
      allow read, write: if request.auth != null 
        && request.auth.uid == userId
        && request.resource.size < 10 * 1024 * 1024;  // Máximo 10MB
    }
  }
}
*/
